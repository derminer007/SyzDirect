index 54e0f8014..d4f691dfd 100644
--- a/workdir/srcs/case_0/net/tipc/netlink_compat.c
+++ b/workdir/srcs/case_0/net/tipc/netlink_compat.c
@@ -31,6 +31,7 @@
  * POSSIBILITY OF SUCH DAMAGE.
  */
 
+#include <linux/kcov.h>
 #include "core.h"
 #include "bearer.h"
 #include "link.h"
@@ -166,6 +167,7 @@ static struct sk_buff *tipc_get_err_tlv(char *str)
 	int str_len = strlen(str) + 1;
 	struct sk_buff *buf;
 
+	kcov_mark_block(2);
 	buf = tipc_tlv_alloc(str_len);
 	if (buf)
 		tipc_add_tlv(buf, TIPC_TLV_ERROR_STRING, str, str_len);
